# GraphQL Playground

Each Openship instance has a GraphQL Playground at `/api/graphql`. Use it to get the API reference, interact with the API, and build integrations.

For testing, use the development instance playground: https://devs.myopenship.com/api/graphql
